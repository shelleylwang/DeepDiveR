#' Plot deepdive_input
#'
#' 'dd_inputs_plot()' makes and saves a variety of plots to start exploring your
#' input data which will save in the file _____.
#' @param dd_input Deepdive_input object made by the function 'prep_dd_input()'
#' which comprises the duration and midpoints of time bins, the number of 
#' localities through time per area and the number of occurrences through time 
#' for each species per area.
#' @returns A file containing ___________ plots.
#' @examples
#' 
#' @export
plot_dd_input <- function(dd_input){
  
}

#' Plot FADS and LADS
#' 
#' 'plt_fads_lads()' plots the first appearance datum and last appearance datum
#' of the occurrences through time.
#' @export
plt_fads_lads <- function(){
  
}

#' Plot number of localities per area through time
#' 
#' 'plt_locs()' makes a line graph of the number of localities per region 
#' through time
#' @export
plt_locs <- function(){
  
}

#' Plot number of species through time (uncorrected)
#' 
#' 'plt_div()' plots the raw taxa counts (unadjusted for biases) through time.
#' @export
plt_div <- function(){
  
}

